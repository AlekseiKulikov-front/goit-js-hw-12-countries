<h1 class="country"> {{name}}</h1>
<div class="country_form">
    <ul>
        <p class="capital"><span class="text">Столица: </span>{{capital}}</p>
        <p class="population"><span class="text">Насиление: </span>{{population}}</p>
        <ul class="languages"><span class="text">Язык:</span>{{#each languages}}
            <li class="tag-list__item">{{name}}</li>
            {{/each}}
        </ul>
    </ul>
    <ul><img src="{{flag}}" alt="" class="flag" /></ul>
</div>